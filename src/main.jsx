import React from 'react'
import ReactDOM from 'react-dom/client'
import DecisionTree from '../decision_tree.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DecisionTree />
  </React.StrictMode>,
)
